# Run HUD status panel — Variant B

**Chosen:** B — combat RO-lite plate.

## Layout

Top-left plate (max **30%** width, **22%** height):

1. Left — class portrait icon (`hub/icon_*.png`) in gold-bordered tile + short label (`лук` / `маг` / `пал`)
2. Right — wide **HP** bar (green, numbers overlaid) then **SP** bar (sky blue)
3. One meta line under bars: `XP base/job · Золото N · biome`

Tokens from `HubTheme` (`overlayEdge` glass, `hp`/`sp`, accent gold border). No cream, no RO window chrome.

Rejected: A glass hub card, C frameless bars, D golden fantasy frame.
